extends layout

block content
    div.container
        h3= 'Для использования системы необходима авторизация'
        p Сейчас будет выполнен вход через Google

    script(type='text/javascript')
        $(function() {
            setTimeout(function(){
                window.location = "/auth/google";
            }, 1000);
        });